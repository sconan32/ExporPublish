using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Socona.Expor.Algorithms
{
    //public class Average:LW
    //{
    //    protected override void UpdateDm(int p, int q, int r)
    //    {
    //        double dist;
    //        foreach (var v in unmergedClusters)
    //        {
    //            if (r == v)
    //            {
    //                continue;
    //            }
    //            dist = (clusterSize[p] * dm[p, v] + clusterSize[q] * dm[q, v]) / clusterSize[r];
    //            dm.Add(new KeyValuePair<int, int>(r, v), dist);
    //        }
    //    }
    //}
}
